export interface CountryData {
  flags: {
    alt: string;
    png: string;
  };
  name: {
    common: string;
  };
  population: number;
  area: number;
  independent: boolean;
  unMember: boolean;
  region: string;
}
